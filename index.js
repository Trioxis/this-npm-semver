var fs = require('fs');

exports.getPackageKey = function(pathToPackageJson){
  return new Promise((resolve,reject)=>{
    // Logic goes here!
    // resolve('the package and version');
    reject(new Error('Not yet implemented'));
  });
};
